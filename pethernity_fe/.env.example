# Unica variabile build-time del frontend: punto di ingresso del backend.
# Tutto il resto (Firebase Web SDK, ecc.) viene servito a runtime dal backend
# via GET /config — vedi src/api/config.ts.
VITE_API_URL=http://localhost:3030
